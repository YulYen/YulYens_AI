# --- Core runtime ---
# Gradio 6 (#61a). Der Sprung ist sicherheitsgetrieben: unter 5.50 trug der
# Stand 30 bekannte Schwachstellen, und **alle** hingen an Gradios eigenen
# Deckeln (`pillow<12.0`, `starlette<1.0`). 6.x hebt beide und behebt zugleich
# die sieben Gradio-Befunde; siehe audit_allowlist.yaml.
#
# Zwei Bruchstellen, die der Sprung erzwungen hat und die man beim
# Zurueckdrehen wieder faende: `gr.Chatbot` kennt kein `type` mehr (das
# Paarformat ist weg, siehe webui_format.ChatMessage), und `gr.Blocks(js=…)`
# wirkt nicht mehr — das Lade-Skript geht an `launch()`.
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.30.6
gradio==6.22.0
gradio_client==2.6.0     # gehoert zu gradio 6.22, zusammen heben
pydantic==2.12.3
# Deckel `<1.0` ist mit #61a entfallen: er stammte daher, dass gradio 4.44
# `HfFolder` importierte (in hf_hub 1.0 entfernt). Gradio 6 verlangt selbst
# 1.x, der alte Pin waere unerfuellbar.
huggingface_hub>=1.0,<2.0
ollama>=0.1,<1.0         # Python-Client fuer Ollama
requests>=2.31,<3.0
PyYAML>=6.0,<7.0
colorama>=0.4.6,<0.5
beautifulsoup4>=4.12,<5.0
spacy>=3.7,<4.0
annotated-types==0.7.0   # Ergaenzt fuer Reproduzierbarkeit

# Test-/Dev-Tooling (pytest, black, ruff, pre-commit): requirements-dev.txt
