# --- Core-Einstellungen ----------------------------------------------------------
core:
  # Standard-Modelname für den Start
  model_name: "leo-hessianai-13b-chat.Q5"
  warm_up: false

# --- UI-Einstellungen ----------------------------------------------------------
ui:
  type: web   # Optionen: "web", "terminal", null (= nur API)
  web:
    host: "0.0.0.0"
    port: 7860

texts:
    project_name: "Yul Yen’s AI Orchestra"
    # Begrüßungstext – Platzhalter in { } werden zur Laufzeit ersetzt
    # Verfügbare Platzhalter: {model_name} und {persona_name}
    greeting: "Chat mit {persona_name} auf Basis der Modellversion {model_name}:"
    choose_persona: "Wähle zuerst eine Persona:"
    new_chat: "🔄 Neue Unterhaltung"
    input_placeholder: "Schreibe…"
    no_selection_warning: "Bitte zuerst eine Persona auswählen."
    persona_button_suffix: " wählen"
    ask_placeholder: "Frag {persona_name} etwas …"
    empty_question: "Bitte stell mir eine Frage 🙂"
    footer_hint: "Powered by {project_name}"
    wiki_lookup_prefix_online:  "🕵️ {persona_name} wirft einen Blick in die echte Wikipedia:"
    wiki_lookup_prefix_offline: "🕵️‍♀️ {persona_name} blättert im lokalen Wikipedia-Archiv:"

# --- Wiki-Einstellungen --------------------------------------------------------
wiki:
  # Ob das lokale Offline-Wiki (Kiwix) genutzt wird: offline
  # Oder Wikipedia: online
  # Oder kein wiki-Tool: false
  mode: offline
  # Immer notwendig, egal ob offline oder online
  proxy_port: 8042
  # Maximale Länge der Wiki-Snippets (Zeichen): 1600 empfohlen
  snippet_limit: 1600
  timeout_connect: 5.0 # <-- EIN Timeout-Set, gilt für online & offline
  timeout_read: 8.0
  offline:
    kiwix_port: 8080 # nur für offline Wiki
    zim_prefix: "wikipedia_de_all_nopic_2025-06" # nur für offline Wiki

# --- Logging -------------------------------------------------------------------
logging:
  # DEBUG, INFO, WARNING, ERROR
  level: "INFO"
  to_console: false
  dir: "logs"
  conversation_prefix: "conversation" 

# --- One-Shot-API z.B. für Tests  -------------------------------------------------------------------
api:
  enabled: true
  host: "127.0.0.1"
  port: 8013