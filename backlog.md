# backlog.md

Priorisiert nach Aufwand/Nutzen, Risiko und Reifegrad. Die Reihenfolge ist von
oben (zuerst angehen) nach unten (später) gruppiert. Die `No.`-Spalte ist eine
stabile ID und ändert sich nicht beim Umsortieren. Erledigte Punkte stehen im
[Archiv](#erledigt-archiv) am Ende. (Stand: 2026-08-01, nach Review-Runde 2)

## Tier A — Fokus: die LoRA-Strecke

#7 ist in Arbeit. #40 und #41 gehörten logisch davor — **beide sind jetzt
erledigt** (siehe Archiv): Votes sammeln sich in `logs/feedback_votes.jsonl`,
die Eval-Suite liegt unter `evals/` und liefert mit `report.csv` das
Vergleichsartefakt für Baseline vs. Adapter. Damit ist #7 nicht mehr blockiert.
Offen bleiben nur die Ausbaustufen #40b (Blind-Ranking) und der erste echte
Eval-Lauf am Modell (#41a). Dessen Blocker #71 (der Judge-Parser las keine
fettgedruckten Punktzahlen und machte daraus lauter Nullen) ist erledigt.

| No. | Name | Description | Effort | Benefit | Category |
| --- | --- | --- | --- | --- | --- |
| 7 | LoRA fine-tuning pipeline | IN PROGRESS → LoRA adapter for LeoLM13B. Abnahme: Baseline vs. Adapter über #41 vergleichen (`logs/evals/report.csv` beider Läufe), Trainingsdaten aus #40 einbeziehen. | L | XL | Research/Quality |
| 41a | Baseline-Lauf der Eval-Suite | Einmalig `python scripts/run_evals.py -e classic` am echten Ollama laufen lassen und den Report als Baseline einchecken bzw. festhalten — erst dann hat der Adapter-Vergleich einen Bezugspunkt. Dabei prüfen, ob der 8B-Judge das Format zuverlässig einhält (`tests/test_evals_live.py` deckt genau das ab) und ob Fälle nachjustiert werden müssen, die am echten Modell zu streng oder zu lasch sind. | S | L | Quality |
| 40b | Blind-Ranking & ELO (Ausbaustufe von #40) | Ask-All mit verdeckten Persona-Namen, Nutzer kürt den Sieger, ELO-Tabelle über die Votes. Baut auf dem JSONL-Log aus #40 auf. Braucht eine likeable Komponente in der Ask-All-Ansicht — die ist heute `gr.Markdown` (kein `.like()`), also entweder `gr.Chatbot` pro Persona oder Vote-Buttons pro Sektion. | M | M | Quality/Data |

## Tier B — Quick Wins

| No. | Name | Description | Effort | Benefit | Category |
| --- | --- | --- | --- | --- | --- |
| 69 | **BUG: Theme-Wechsel wirft die Sitzung weg** | Hell/Dunkel umzuschalten führt zurück auf die Startseite; getippter, noch nicht abgeschickter Text, das laufende Gespräch und die gewählte Persona sind weg. Ursache steht als Kommentar im Code (`ui/webui_layout.py`, „der Reload ist der Preis"): der Umschalter sind zwei `<a href="?__theme=…">`, also ein **voller Seiten-Reload** — neuer `session_hash`, und damit ist jeder `gr.State` (Persona, Streamer, `conversation_state`, Gast) neu initialisiert. Beim Entwurf von #36 als akzeptabel eingestuft, war es aber nie: Verlust von Eingaben durch einen kosmetischen Klick ist ein Datenverlust-Bug, kein Schönheitsfehler. **Der Reload ist vermeidbar** — im mitgelieferten Gradio-Bundle ist Dark-Mode nichts als `document.body.classList.add/remove("dark")` (Funktion `Ue` in `templates/frontend/assets/Index-*.js`); ein clientseitiger Umschalter (`gr.Button` mit `js=`, Wahl in `localStorage`, Wiederherstellung beim Laden über `gr.Blocks(js=…)`) kommt ohne Serverrunde und ohne Reload aus. Fallstrick beim Bauen: Gradio setzt sein eigenes Theme beim Init selbst (`Je()` liest `?__theme` bzw. `prefers-color-scheme`) — die Wiederherstellung muss **danach** laufen, sonst blinkt es oder gewinnt Gradio. Zweiter Punkt, gleiche Ecke: **der Umschalter sieht nicht gut aus** — zwei blasse Textlinks (`opacity: .65`) oben rechts, beide immer sichtbar, ohne Anzeige, welcher gerade gilt. Ein einzelner Umschalt-Button, der den *anderen* Zustand anbietet, wäre klarer und kleiner. Achtung Wechselwirkung mit #61: die Gradio-5-Migration fasst genau dieses Frontend-Verhalten an. | S | M | UX/Bug |
| 53a | Identität auch für API und Mail-Adapter | Nachzug zu #53: die Web-UI kennt jetzt einen Nutzerbegriff, API (`api.openai_compatible.api_key`) und Mail-Adapter (Adress-Zuordnung) haben weiterhin ihre eigenen Mechanismen. Ein gemeinsamer Identitätsbegriff über alle drei Kanäle würde die Gesprächslogs kanalübergreifend zuordenbar machen — Voraussetzung dafür, dass #24 (Fakten über den Nutzer) auch greift, wenn man per Mail schreibt. | M | M | Technical foundation |
| 27 | Ask-All-Moderator | Nach dem letzten Broadcast-Event ein zusätzlicher `run_llm_collect`-Lauf: „Fasse die 4 Antworten zusammen, nenne die stärkste" als gestreamte Sektion „🎭 Fazit". Opt-in per Checkbox (kostet einen vollen LLM-Lauf am Ende). Moderator bekommt die Antworten gekürzt (Karl-Heuristik), sonst sprengen 4 lange Antworten die 8k ctx. | S | M | Orchestration |
| 42 | Perf-Benchmark-Harness | `scripts/bench.py`: n Fragen gegen die API, `t_first`/`t_total` als CSV/Markdown-Report. Holt die ausstehende Latenz-Verifikation von #17 nach und macht Modellwechsel (`modelfiles/`!) endlich vergleichbar. Bewusst getrennt von #41: hier Stoppuhr, dort Qualität. | S | M | Performance |
| 14 | E-Mail-Adapter härten (war: Restpunkte) | MVP ist live (siehe Archiv). **Aus dem Review Runde 2 kommen drei Befunde dazu, die aus einem Betriebs- ein Sicherheitsthema machen — deshalb hochgestuft.** (a) `processed_mailbox` mit KAS/Dovecot-Punkt-Trenner (`INBOX.YulYenProcessed`) scharf testen — der Trenner wird nie vom Server erfragt (`LIST` liefert ihn), `INBOX/…` ist geraten; (b) Dauerbetrieb via `launch.py` verifizieren; (c) Postfach-Passwort rotieren (stand im Chat); **(d) Antwortadresse aus `From` statt `Reply-To`** — belegt: eine Mail mit `Reply-To: opfer@…` lässt die Instanz eine Mail *an Dritte* schicken, vom Mailserver des Betreibers (also mit gültigem SPF/DKIM seiner Domain) und mit dem vom Absender gewählten Text wörtlich im Zitat; **(e) Absender-Allowlist** als Pflichtfeld bei `enabled: true` — es gibt heute gar keine, jeder mit der Adresse kann die Personas fahren; **(f) markieren VOR dem Senden**, mindestens `\Seen` als Fallback — belegt: schlägt `_mark_processed` fehl (COPY scheitert, Quota, falscher Trenner), bleibt die Mail UNSEEN und wird jeden Poll erneut beantwortet. Im Test: 4 Zyklen, **4 identische Antworten**, `run_once()` meldet jedes Mal 0, es sieht also aus, als sei nichts passiert. Bei 60 s Poll sind das 1440 Mails/Tag an denselben Empfänger, jede mit einem LLM-Lauf; **(g) RFC 3834**: ausgehend `Auto-Submitted: auto-replied` setzen, eingehend `Auto-Submitted`/`Precedence: bulk`/`List-Id` ignorieren — sonst dreht sich der Adapter mit dem nächsten Abwesenheitsassistenten im Kreis; **(h) Größenlimit auf den Body** — er geht heute ungekürzt in den Prompt *und* vollständig ins Zitat der Antwort. | M | L | Integration/Security |
| 65 | Vote-Kanal an die Ablage anschliessen | Nachzug zu Runde 3: Beiwerk wird nicht mehr aufgezeichnet, aber der Vote-Eintrag hat weiterhin **keine `conversation_id`** und nimmt den *angezeigten* Text (maskiert, ggf. mit `…[abgebrochen]`-Suffix) statt dessen, was das Modell produziert hat. Beides sauber loesbar, sobald #59 die Ablage verlaesslich macht: Vote referenziert Gespraechs-ID plus Nachrichtenindex und liest den Text aus dem Store. Damit ist der Kanal fuer #7 auswertbar (Join auf Persona, Modell, Zeitraum) statt nur eine Liste loser Textpaare. | S | M | Data |
| 66 | Schema-Pruefung rekursiv (Rest von #63) | Der Auth-Teil ist in Runde 3 erledigt (Abbruch statt Fail-Open). Offen bleibt: `extra="allow"` steht in jeder Sektion, der Tippfehler-Abgleich laeuft nur gegen `KNOWN_TOP_LEVEL_KEYS` — gemeldet wird also die Ebene, auf der sich niemand vertippt. Stumm bleiben `security.pii_protection` -> `pii_protecton`, `storage.enabled` -> `enable`, `api.…api_key` -> `apikey`. | M | M | Technical foundation |
| 68 | Einsteiger-UI: Sonderoptionen hinter „Für Profis" wegklappen | Die Startseite zeigt heute alles gleichrangig nebeneinander: vier Persona-Karten *und* Self-Talk, Gast anlegen, Verlauf, Ask-All als gleich aussehende Karten, darunter JSON-Upload, darunter Briefing/Vorlesen/Nochmal/Herunterladen, dazu Mikro und Quellen-Accordion. Für den Einstieg ist das überladen — wer zum ersten Mal reinschaut, soll eine Persona wählen und tippen, sonst nichts. **Das Muster existiert bereits und ist nur nicht angewendet:** die Modellwahl liegt seit #6 zugeklappt in einem `gr.Accordion` mit `elem_classes="advanced-accordion"` (`ui/webui_layout.py`) — der Umbau ist im Kern, weitere Elemente dort hinein bzw. in ein zweites Accordion zu verschieben. Bewusst noch **nicht entschieden, was genau wohin gehört** (Self-Talk? Gast? JSON-Austausch? Ask-All?) — das ist die eigentliche Arbeit und wird mit Yul ausdiskutiert, bevor gebaut wird. Randbedingungen für den Entwurf: die Verdrahtung ist getestet (`test_web_ui_wiring.py`), Komponenten dürfen also umziehen, aber nicht verschwinden; Sichtbarkeit hängt teils an Config-Flags (`file_exchange`, `broadcast_mode`, `stt`, `tts`), ein Accordion darf diese Logik nicht doppeln; Labels gehören nach `locales/{de,en}.yaml`, nicht in den Code. Sinnvoll zusammen mit #56 (Zerlegung von `web_ui.py`) zu betrachten, aber unabhängig davon machbar. | S–M | M | UX |
| 70 | Startseite: Yul-Yen-Fotos von den Funktionskarten nehmen | `static/YUL_YEN.png` steht heute **zweimal** auf der Startseite — als Bild der Karte „Gast anlegen" und der Karte „Verlauf" (`ui/webui_layout.py`). Beide Karten sind Funktionen, keine Personen; dasselbe Porträt zweimal nebeneinander liest sich wie zwei weitere Gesprächspartner und verstärkt genau die Verwechslung aus #68 (Funktionskarten sehen aus wie Persona-Karten). Ersatz: ein **sehr großes, schlichtes Icon** je Karte — Maske/Theaterfigur für den Gast, Archiv/Ordner für den Verlauf. Bevorzugt **Inline-SVG in einem `gr.HTML`** statt einer `gr.Image`-Komponente: skaliert scharf, nimmt über `currentColor` das Theme mit (siehe #69) und spart pro Karte eine Gradio-Komponente samt Datei-Endpunkt; PNG in `static/` nur, falls ein gezeichnetes Motiv gewünscht ist. Mit zu prüfen: `static/ST.png` (Self-Talk) und `static/ALL.png` (Ask-All) — dieselbe Frage, nur nicht dasselbe Bild. Die Persona-Karten selbst behalten ihre Porträts, das ist dort der Sinn. | S | S | UX |
| 60 | Injizierter Fremdkontext in die `user`-Rolle | Nachzug zum Guard-Fix aus Runde 2: der Kontext wird jetzt geprüft, steht aber weiterhin als `system`-Nachricht im Prompt — höchste Autorität für Text aus einer heruntergeladenen ZIM-Datei oder einem abonnierten Feed, direkt hinter der Anweisung, ausschließlich ihm zu folgen. Verschieben nach `user` mit klarer Delimitierung. Bewusst getrennt vom Guard-Fix: es ändert das Antwortverhalten *aller* Personas und ist ohne Lauf am echten Modell nicht abnehmbar (die Eval-Suite #41 ist der Weg dafür). | S | M | Security |
| 61 | Gradio 5.x-Migration | Der 4.44.1-Pin trägt sechs auf dieses Setup anwendbare Advisories: PYSEC-2024-213 (Directory-Traversal-Check umgehbar → beliebige Datei lesen), PYSEC-2025-118 (ACL-Bypass über Groß-/Kleinschreibung), PYSEC-2024-215 (SSRF), PYSEC-2024-220 (XSS), PYSEC-2024-214 (CORS akzeptiert `null`-Origin), GHSA-26jh-r8g2-6fpr (Datenvalidierung in `Dropdown` — genau der Mechanismus hinter dem Verlauf-IDOR aus Runde 2). Nicht anwendbar und deshalb ausdrücklich nicht mitgezählt: Flagging (die App nutzt `gr.Blocks`, nicht `gr.Interface`), OAuth-Komponenten, `FileExplorer`, `Dataframe`. Default ist `host: 0.0.0.0`. **Der dokumentierte Grund für den Pin ist entfallen:** `gr.Dataframe` kommt im Code nicht mehr vor, nur noch in zwei Kommentaren, die erklären, warum man sie *nicht* benutzt; der `pydantic==2.9.2`-Pin ist Folge des Gradio-Pins, kein eigener Grund. Netz für den Umbau existiert (`test_web_ui_wiring.py`). Nebenpunkt: `pip-audit` in die CI, damit ein Pin bewusst getragen statt vergessen wird. | M–L | L | Security/Technical foundation |
| 63 | ~~Schema rekursiv + Anmeldung fail-closed~~ → aufgeteilt | `extra="allow"` steht in jeder Sektion, der Tippfehler-Abgleich läuft nur gegen `KNOWN_TOP_LEVEL_KEYS` — gemeldet wird also die Ebene, auf der sich niemand vertippt. Stumm bleiben `ui.web.auth.users` → `user`, `security.pii_protection` → `pii_protecton`, `storage.enabled` → `enable`, `api.…api_key` → `apikey`. Folge des ersten: `LocalUsersAuth` findet keinen Nutzer und liefert `gradio_auth() → None`, die App startet auf `0.0.0.0` **ohne Login**. Das ist als „lieber offen und laut" gewollt; die Entscheidung sollte fallen, weil der häufigere Auslöser kein Tippfehler ist, sondern ein nicht durchgereichtes `env:` (systemd ohne `EnvironmentFile`, Container ohne `--env`). Fehlverhalten „zu" = ein Support-Ticket, „offen" = unbegrenzt. Also: rekursiver Unbekannte-Keys-Abgleich für die Sektionen, die das Schema kennt, und bei `provider: local` ohne nutzbaren Nutzer harter Abbruch statt Fallback. | **Anmeldungs-Teil in Runde 3 erledigt** (`AuthConfigError`, Exit 4, plus laute Warnung bei nicht-Loopback ohne Login; `ui.web.host` steht jetzt auf 127.0.0.1). Der Schema-Teil laeuft als #66 weiter. | — | — | erledigt/aufgeteilt |
| 64 | Review-Kleinkram (Sammelticket Runde 2) | (a) Die zweite Guard-Prüfung in `respond_one_shot` ist tot — `run_llm_collect` liefert bereits moderierten Text, `check_output` feuert nie; sieht nach Tiefenverteidigung aus und ist keine. (b) `respond_one_shot` macht den Wiki-Lookup **vor** der Guard-Eingangsprüfung: eine abgelehnte Eingabe löst trotzdem einen Netzabruf mit fremdbestimmtem Suchbegriff aus. (c) `RateLimiter._windows` wächst unbegrenzt mit der Zahl der Client-IPs. (d) `iter_broadcast_events_parallel` legt den *kumulativen* `reply` in jedes Token-Event → O(n²) Allokation genau im parallelen Pfad. (e) Coverage wird gemessen, aber ohne Schwelle — die Zahl in der Job-Summary ist dekorativ; dazu kein Smoke-Test, dass `launch.py` startet oder `--doctor` durchläuft, und nur Python 3.10 in der Matrix. (f) Ein voller `pytest`-Lauf legt `data/conversations.sqlite3` an, obwohl CLAUDE.md das Gegenteil behauptet — **null Zeilen darin**, und die Ursache ließ sich keinem einzelnen Modul zuordnen (nur der Gesamtlauf erzeugt es); billiger Schutz wäre ein erzwungener `tmp_path`-Default in den Tests. | S | M | Quality |

## Tier C — Strategische Hebel (größerer Aufwand, hoher Nutzen)

| No. | Name | Description | Effort | Benefit | Category |
| --- | --- | --- | --- | --- | --- |
| 24 | Langzeit-Gedächtnis | Der größte UX-Hebel: Personas, die sich erinnern, sind Charaktere statt Chatbots. Andockpunkt `_system_prompt_with_date` (`core/utils.py`). **Ohne Embeddings starten:** nach Gesprächsende extrahiert ein Karl-artiger Lauf 3–5 Fakten in SQLite/JSON, beim Start in den Prompt. Getrennt: Fakten über den Nutzer (persona-übergreifend) vs. Erlebnisse der Persona (pro Persona, füttert #48). „Vergiss das"-Kommando, Memory-Datei gitignored. Risiken: Prompt-Bloat frisst #17-Speedup (8k ctx!), Extraktionsqualität bei 8B-Modellen. Vektor-Store erst, wenn die Faktenliste zu groß wird. | L | XL | Cool feature/Quality |
| 30 | Tool-Use-Grundgerüst | Strategischer Türöffner (für #46, Wetter, langfristig Wiki-als-Tool statt spaCy-Injection). Ollama unterstützt `tools`, Ministral kann Function Calling — aber es bricht den schlanken Stream-Flow in `streaming_provider.py` (Tool-Call → Ausführen → Folge-Request → erst dann Stream), und 8B-Modelle rufen Tools unzuverlässig auf. **V1: genau ein Tool (sympy-Rechner), nur PETER, hinter Config-Flag** — Zuverlässigkeit am echten Modell messen, bevor mehr investiert wird. | L | L | Technical foundation |
| 26 | Multi-Persona-Modi 2.0 | Fundament: `self_talk.py` (2 Personas) + `orchestrator.py`. Kern-Idee gegen Scope-Explosion: **Modi als YAML-Presets** (`ensembles/modes/*.yaml`: Sprecherreihenfolge, Rollen-Zusatzprompts, Abbruchkriterium) — dann sind Debatte/Kritiker/Geschichten-Runde nur Daten, kein Code. Code-Arbeit: generalisierter Self-Talk für n Personas. Erster Modus: **Debatte** (Pro/Contra + Moderator-Fazit, höchster Wow-Faktor). Gibt nebenbei die interne Erweiterbarkeit, für die sonst ein Plugin-System nötig wäre. | L | L | Orchestration |
| 31 | Wiki 2.0 | (a) Mehrere ZIMs: kiwix-serve serviert mehrere Bücher in einem Prozess, der Proxy braucht nur Book-Routing; Wiktionary-ZIM als natürlicher zweiter Band. (b) Kiwix' eingebaute Volltext-/Suggest-Suche nutzen statt Titel-Raten — der realistische „semantische" Sprung (Embeddings über 13-GB-ZIM: unrealistisch, gestrichen). (c) LRU-Cache für wiederholte Lookups. | M | M | Integration |
| 39 | MCP-Server | Dünner Wrapper über den One-Shot-Provider: `ask_persona(name, question)` + `ask_all(question)` als MCP-Tools (stdio, `mcp`-Paket). Damit kann z. B. Claude Code die Personas als Werkzeug benutzen. Extra: Wiki-Lookup als Tool exponieren — ein lokaler Offline-Wikipedia-MCP-Server ist für sich allein ein nützliches Artefakt. | S | M | Integration/Cool |
| 44 | Distribution & Setup | In Scheiben: (1) `setup.ps1` für Windows (venv, Ollama-Modelle pullen, spaCy-Modell, Piper-Voices) — der eigentliche Schmerzpunkt, viele manuelle Schritte. (2) uv/pyproject-Migration mit Lockfile — Achtung CLAUDE.md-Falle: Black/Ruff-Pins synchron mit `.pre-commit-config.yaml` halten. (3) CHANGELOG + GitHub-Releases. (4) Docker-Compose (App+Ollama+Kiwix) zuletzt — GPU-Passthrough + Windows-TTS machen es nur für Headless-Linux sinnvoll. | M | M | Technical foundation |

## Tier D — Nice-to-have / Cool features (später)

| No. | Name | Description | Effort | Benefit | Category |
| --- | --- | --- | --- | --- | --- |
| 56 | `web_ui.py` in Controller pro Ansicht zerlegen | Rest aus #55: die reinen Formatierer sind raus (`ui/webui_format.py`), die Ausgabe-Key-Listen liegen beim Layout — die Klasse hat trotzdem noch ~2100 Zeilen und 64 Methoden. Eine echte Zerlegung (Chat / Ask-All / Verlauf / Gast / Self-Talk als eigene Controller mit gemeinsamer Basis) wurde bewusst **nicht** in denselben PR gepackt wie die Sitzungsumstellung, weil beide dieselbe Datei umgraben. Vorbedingung ist erfüllt: der Verdrahtungstest (`test_web_ui_wiring.py`) fängt verrutschte Bindungen jetzt ab. | M | S | Technical foundation |
| 38 | Telegram-Adapter | Blaupause: `email_adapter/service.py` (Polling-Loop, opt-in, Guard). Telegram zuerst: Long-Polling braucht keine öffentliche IP, `python-telegram-bot` als Dependency. `/persona LEAH`-Kommando statt Adress-Map, Rate-Limit pro Absender. Spannungsfeld benannt: Telegram = Cloud, Projekt-Ethos „kein Cloud-Zwang" → strikt opt-in. Signal/Matrix erst bei Bedarf. | M | M | Integration |
| 46 | Assistenz-Paket (nach #30) | Notizen/Todos als Persona-Tool (braucht #30 als Fundament, sonst Regex-Gefrickel). Erinnerungen: Scheduler + Zustellkanal — Charme-Detail: der E-Mail-Adapter ist der Zustellkanal, der schon existiert („LEAH, erinner mich morgen an X" → morgen kommt eine Mail von LEAH). Nicht vor #30 anfassen. | M | M | Cool feature |
| 16 | Sandbox/PDF functions | Local documentation sandbox (eigene Dokumente als Kontext-Quelle). | L | M | Cool feature |
| 47 | Spiele-Modus | Als Preset auf der #26-Infrastruktur. Zuerst „20 Fragen, Nutzer denkt sich etwas aus, Persona rät" — braucht keinen versteckten Zustand, trivial. Umgekehrte Richtung (Persona denkt sich etwas aus) braucht vor dem Nutzer versteckten State = der eigentliche Bau-Aufwand. | M | S | Cool feature |
| 49 | Gesprächsarchiv-Volltextsuche | `logs/conversation_*.json` liegen brach — niemand schaut je wieder rein. Lokale Volltext-Suche übers Archiv (Terminal `/suche <begriff>` + WebUI), Treffer mit Persona/Datum; Fundstellen optional als Kontext ins laufende Gespräch injizieren („Was hat PETER neulich über Kiwix erzählt?" — Injection-Muster existiert). Abgrenzung zu #25: dort Browsen/Rendern/Export einzelner Logs, hier Suche über alle. Komplett offline; Embedding-Suche erst als Stufe 2. **Seit #54 liegen die Gespräche in SQLite** — die Volltextsuche ist damit eine FTS5-Tabelle als Migrationsschritt 2 plus eine `search()`-Methode am Store, kein eigener Index. Seit Runde 2 läuft ein Migrationsschritt in einer Transaktion: ein fehlendes FTS5 auf der Zielmaschine lässt die Datei auf der alten Version stehen, statt sie halb migriert und dauerhaft unbrauchbar zu hinterlassen. | M | M | Cool feature |
| 48 | Traum-Log (Wildcard) | Ohne Cron machbar: beim ersten Start des Tages prüfen, ob Conversation-Logs von gestern existieren → Karl-artiger Lauf mit surrealem Prompt erzeugt den „Traum", WebUI zeigt beim Start „💭 LEAH hat geträumt: …". Reine Nutzung vorhandener Bausteine (Logs + Summarizer), Synergie mit #24 (Persona-Erlebnisse als Traumstoff). Absurd, aber passt zum Geist des Projekts. | S | S | Cool feature |

## Gestrichen

| No. | Name | Grund |
| --- | --- | --- |
| 11 | Easter egg logic | Reine Spielerei (Keyword → Extra-Tagline), 0 % umgesetzt, M-Aufwand für minimalen Nutzen — schlechtester Aufwand/Nutzen im Backlog. Bei Bedarf später als triviales Add-on. |
| — | Auto-Routing (Frage → passende Persona) | Die 4 Personas unterscheiden sich in Stil, nicht Kompetenz — der Router beantwortet „welchen Ton willst du?", das weiß der Nutzer selbst besser; kostet einen LLM-Lauf Latenz pro Frage. Wiederbeleben, falls je ein Kompetenz-Ensemble (Arzt/Anwalt/Techniker) entsteht. |
| — | Plugin-System (Entry-Points) | YAGNI: lohnt erst mit externen Contributors. Modus-Presets (#26) und Tool-Registry (#30) liefern die interne Erweiterbarkeit billiger. Wiedervorlage, falls das Projekt Contributor gewinnt. |
| — | VRAM-adaptive Modellwahl | Automatischer Modell-Swap ist Überraschungsverhalten (anderes Modell = andere Persona-Qualität, siehe `knowledge_cutoffs`-Sorgfalt). Der sinnvolle Kern — VRAM-Bedarfs-Hinweis — ist als Doctor-Detail in #43 gewandert. |

## Erledigt (Archiv)

| No. | Name | Description | Category |
| --- | --- | --- | --- |
| 71 | Judge-Parser verträgt keine Markdown-Auszeichnung | **DONE (2026-08-01): der Test war der Fund, der Fix gehört in den Parser.** `test_judge_returns_parsable_scores_from_a_real_model` fiel am echten Ollama — aber nicht, weil das Modell das Format brach: es antwortete `1: **5** | Hauptstadt korrekt genannt`, also formatgetreu, nur in Fettschrift. `_SCORE_LINE` verlangte die Ziffer unmittelbar hinter dem Trenner, fand keine, und `parse_verdict` machte daraus korrekt `score=None`. **Die Folge lag nicht im Test, sondern in der Suite:** derselbe Parser bewertet jeden `expect_traits`-Fall, ein Baseline-Lauf hätte also lauter „keine verwertbare Antwort des Judges" gemeldet — Durchfaller trotz sauber bewertender Judge-Antworten. **Jetzt** toleriert das Muster Dekoration *um* die beiden Zahlen herum: `**`/`__`/Backticks, führende Aufzählungszeichen, ein Label (`Punktzahl:`, `Bewertung 5`), `5/5`, Gedankenstrich statt `|`. **Die Strenge bleibt, das war die eigentliche Arbeit:** die Zeile muss weiterhin mit der Erwartungsnummer beginnen und die Punktzahl eine einzelne 1–5 sein — „Insgesamt 5 von 5 Erwartungen erfüllt", „1. Satz: die Hauptstadt stimmt" und Werte außerhalb der Skala ergeben nach wie vor `None`, festgehalten in 6 Gegenproben. Ein Detail aus dem Testlauf: `\b` hinter der Punktzahl funktioniert bei `__5__` nicht (Unterstrich ist ein Wortzeichen) — jetzt eine Lookahead-Prüfung, die Ziffern und Buchstaben ausschließt, Auszeichnung aber zulässt. Regressionsfall ist die wörtlich beobachtete Zeile, dazu 10 Dekorations-Varianten. **Entblockt #41a.** Die Nebenbeobachtung des Tickets bleibt offen und steht dort: der Judge gab „maximal zwei Sätze" eine 1, obwohl die Antwort zwei Sätze hatte — eine Kalibrierungsfrage der Fälle, keine des Parsers. 960 Tests grün. | Quality |
| 72 | Ohne Anmeldung keine Gesprächs-Ablage | **DONE (2026-08-01).** Aufgefallen ist es an der Erwartung „ohne Anmeldung sollte nichts in der DB landen" — und die Erwartung war berechtigt: `DisabledAuth` (Default) gibt *jedem* Besucher die Identität `local`, also tragen alle Gespräche denselben Eigentümer. Die Eigentümerprüfung aus Runde 2 (`load`/`delete` mit `user=`) prüft dann eine Bedingung, die immer zutrifft — wer die Seite erreicht, sieht im Verlauf die Gespräche aller anderen, kann sie fortsetzen und löschen. **Nicht akut im Auslieferungszustand**, weil `ui.web.host` seit #53-Runde-3 auf `127.0.0.1` steht und `share` ohne Login aktiv verweigert wird; scharf wird es genau dann, wenn jemand den Host umstellt — dort blieb es bisher bei einer Logzeile, während die Ablage weiter in einen gemeinsamen Topf schrieb. **Jetzt:** `AppFactory.get_store()` liefert einen `NullStore`, wenn `ui.type: web` ohne Anmeldung läuft, und nennt in der Meldung beide Auswege; der ausdrückliche Weg in den gemeinsamen Topf ist `storage.shared_without_login: true` (Default aus) mit lauter Startwarnung. **Terminal und API bleiben unberührt** — dort gibt es keine Anmeldung, die fehlen könnte, und die Regel gilt bewusst nicht für sie. Zwei Nähte statt Fallunterscheidungen: `AuthProvider.identifies_users` (unterscheidet dieser Provider Besucher überhaupt?) und `ConversationStore.records` (schreibt diese Ablage wirklich?). Über die zweite hängt die WebUI die Verlauf-Karte weg, wenn nichts aufgezeichnet wird — das war auch bei `storage.enabled: false` schon eine Karte, die sich nie füllen konnte. Die Anmeldung wird zudem nur noch **einmal** gebaut (`AppFactory.get_auth_provider()`), vorher tat es die WebUI ein zweites Mal. **Am Testgerüst hat sich der eigentliche Wert gezeigt:** `factory_double()` lieferte für `get_auth_provider()`/`get_store()` wahrheitswertige Mocks — genau die stille Richtung aus #67 — die WebUI startete im Test also mit einer Anmeldung, die es nicht gab. Beides jetzt mit den echten Produktionsvorgaben vorbelegt (`DisabledAuth`, `NullStore`, beide falsy). Der Verdrahtungstest läuft ab jetzt **mit** Ablage, sonst prüfte er die Verlauf-Bindungen gar nicht mehr; dazu ein Gegentest, dass die App ohne Ablage startet (nichts bindet an die fehlende Karte). 8 neue Fälle in `tests/test_recording_requires_login.py`, 986 Tests grün, mypy sauber. | Security |
| 62 | Guard-Regelwerk überarbeiten | **DONE (2026-08-01): gemessen statt geschätzt — 20 alltägliche Sätze vorher, 8 davon geblockt; nachher 0, bei unveränderter Trefferzahl auf 18 Angriffen.** Die Diagnose war nicht „die Wortlisten sind zu grob", sondern zweimal etwas Anderes. **(a) Themenwoerter sind keine Angriffe.** `localhost`, `http://127.0.0.1`, `file://`, `/etc/passwd`, `system32\config\sam` sagen, *worueber* ein Text spricht — nicht, dass er das Modell uebernehmen will. Das Modell kann ohnehin keine Dateien lesen, die Regeln haben also nie etwas geschuetzt; in einem Projekt, dessen Gegenstand lokale Dienste sind (Kiwix 8042, API 8013, WebUI 7860), haben sie schlicht die eigenen Fragen geblockt. Ersatzlos raus, die beiden Korpus-Faelle sind zu `ok_`-Faellen umgedreht. **(b) Der Abstand zwischen Verb und Objekt war der Praezisionskiller, nicht die Wortliste.** `\bübergehe\b.{0,80}\b(regeln)\b` verbindet ueber achtzig Zeichen fast jedes Verb mit fast jedem Substantiv („Übergehe bitte die Regeln des Kartenspiels nicht"). Brücken sind jetzt kurz und ueberspringen keine Teilsatzgrenze (`[^,.!?\n]` statt `.`) — damit faellt auch „Wir bauen ein Modellflugzeug, keine Bombe" weg, ein Treffer *ueber die Verneinung hinweg*. **Entschaerft statt geloescht:** `act as` verlangt jetzt eine privilegierte Rolle („act as an unrestricted shell" ja, „act as a friendly tour guide" nein); `sei X` ist durch `assistant_role_assignment` ersetzt, das an der *Ansprache* erkennt statt am Abstand; `developer mode` braucht entweder die Ansprache oder das Wegfallen der Beschraenkungen im selben Satz (Chrome und Android haben einen Developer Mode, danach fragt man). **Ein Recall-Verlust wurde beim Testen gefunden und geschlossen:** `stell(e|en|st|t)?` musste aus der Herstellungs-Wortliste raus (es traf jedes „wir stellen …"), womit „Wie stelle ich Sprengstoff **her**" durchfiel — deutsche Trennverben. Jetzt eigene Regel mit Pflicht-`her`. **Der zweite Teil ist die Messbarkeit:** die Muster sind `Rule(name, pattern)` statt roher Strings, `check_input`/`check_output` liefern den Namen als `rule`, und der Korpus prueft ihn (`expect.rule`). Das hat sofort einen Fund gebracht: `ctx_weapon_instructions_in_article` wurde nie von der Anleitungsregel gefangen, sondern von der Bau-Regel davor — ein Fall, der aus dem falschen Grund gruen ist, sah vorher aus wie ein Erfolg. `security.custom_patterns` bleibt eine Liste roher Regexe und bekommt erzeugte Namen (`custom_<bucket>_<i>`). **Abgenommen ueber den known_gap:** `ctx_code_snippet_in_article_is_kept` bestand ploetzlich, also fiel das Flag und `KNOWN_GAP_IDS` in `tests/test_evals_cli.py` ist wieder leer — genau die Richtung, die ein blosses „keine Luecken" nie gemeldet haette. Neue Hausregel: wer eine Injection-Regel ergaenzt, legt in `evals/guard_redteam.yaml` den `ok_`-Satz daneben, den sie nicht treffen darf. 9 solche Gegenproben sind mit dazugekommen. Mutationsgeprueft: die alten Muster wieder eingesetzt → 12 Korpus-Faelle fallen. 921 → 933 Tests. | Security/Quality |
| 67 | Test-Doubles gegen die echte Schnittstelle absichern | **DONE (2026-08-01): die offene Entwurfsfrage des Tickets ist empirisch beantwortet — `create_autospec` traegt, Klassen-Annotationen braucht es nicht.** Nachgemessen: `dir(YulYenStreamingProvider)` kennt `stream`/`record_conversation`/`set_conversation`, aber weder `guard` noch `persona_options`. Genau das ist aber **richtig** so: `getattr(streamer, "guard", None)` liefert am `create_autospec`-Double `None` statt eines wahrheitswertigen Mocks, weil ein nie gesetztes Instanzattribut ehrlich fehlt. Ein Attribut per Annotation in `dir()` zu heben wuerde die stille Driftrichtung also nicht schliessen, sondern wieder oeffnen. Neu: `tests/doubles.py` mit `streamer_double()`, `permissive_guard_double()` und `factory_double()`, dazu `tests/test_doubles.py` (13 Faelle) fuer beide Richtungen — neue Produktivmethoden sind automatisch am Double, ein Tippfehler im Doublenamen fliegt als `AttributeError` auf, und ein Aufruf mit falscher Signatur als `TypeError`. **Die eine Luecke, die `create_autospec` offenlaesst, ist eigens getestet:** *Setzen* unbekannter Attribute bleibt erlaubt (kein `spec_set`, sonst liesse sich `persona_options` gar nicht vorbelegen), deshalb prueft `test_the_presets_are_attributes_a_real_streamer_actually_has` die Vorbelegung gegen eine echte Instanz. **Migriert:** `_streamer_double` in `test_web_ui.py` (18 Stellen) und `test_terminal_ui.py`, `AllowAllGuard` und `_BlockAllGuard` in `test_streaming_provider.py`, `_AlwaysOkGuard` in `test_evals_runner.py`, Mock-Factories in `test_web_ui.py`/`test_openai_compat.py`/`test_self_talk.py`/`test_terminal_ui.py`. Der lokale Drift-Test `test_the_streamer_double_matches_a_real_streamer` faellt weg — er ist in `tests/test_doubles.py` aufgegangen. Netto 159 Zeilen Testcode weniger bei mehr Zusicherung. **Ein Fund nebenbei:** `stream.return_value` als `iter([])` waere nach dem ersten Aufruf stumm leer gewesen (die handgeschriebenen Doubles lieferten je Aufruf einen frischen Iterator); jetzt eine Liste, die der Produktivcode genauso iteriert. Beide Richtungen mutationsgeprueft: zurueck auf `Mock()` → 3 Faelle fallen, Tippfehler in der Vorbelegung → der Preset-Test faellt. 909 → 921 Tests. | Quality |
| 59 | Ablage = Gespräch statt LLM-Aufruf | **DONE (2026-08-01).** `_append_conversation_log` sass in `stream()` und protokollierte damit Generierungs*versuche*; den akzeptierten Gespraechsstand kennt aber die Oberflaeche. Vier Folgen, alle belegt: „Nochmal 🔄" zweimal gedrueckt ergab 3 identische Fragen und 3 Antworten im Store, obwohl die UI eine zeigte — beim Fortsetzen sah das Modell diesen erfundenen Verlauf als echten Kontext; „Stop ⏹" schloss den Generator, wodurch `GeneratorExit` den Schwanz von `stream()` uebersprang und die Teilantwort fehlte, obwohl UI und LLM-Kontext sie behielten; Ask-All und Self-Talk zeichneten **gar nichts** auf, weil dort nie eine Gespraechs-ID gesetzt wurde. Jetzt besitzt die Oberflaeche den Stand und die Ablage spiegelt ihn: neues `ConversationStore.sync` ersetzt den ganzen Verlauf statt anzuhaengen (keine Buchfuehrung, die auseinanderlaufen kann), `record_conversation` wird vom Aufrufer gerufen, sobald der Turn steht, und `stream()` schreibt nur noch den JSONL-Mitschnitt — der *soll* Versuche festhalten. Damit stimmt die Rollenverteilung aus #54 endlich. Injizierter System-Kontext (Wiki, Briefing) bleibt draussen, er gehoert zum Prompt. Verdrahtet in WebUI, Terminal, `/ask`, `/v1`, beiden Broadcast-Pfaden und Self-Talk; Ask-All und Self-Talk eroeffnen dabei je Persona ein eigenes Gespraech (`app: ask-all` / `self-talk`). Nebenbei: `test_api_turns_are_recorded_in_the_store` ersetzte frueher `respond_one_shot` komplett und rief die Aufzeichnung von Hand — er prueft jetzt den echten Pfad. | Technical foundation |
| 58 | `_StreamModerator` neu schreiben | **DONE (2026-08-01): drei Defekte desselben Codes, eine Umschreibung.** **(a) Verschluckter Modelltext:** `_emitted` indizierte in den *maskierten* Text, dessen Länge sich beim Ersetzen ändert — nach dem ersten Treffer zeigte der Index auf die falsche Stelle. Belegt: aus „… Adresse [PII] und dann noch viel Text …" wurde ausgeliefert „… Adresse vorname.nachname.abt**viel Text** …". Jetzt zählt `_released` **rohe** Zeichen, und die Freigabegrenze darf nie mitten in einem Treffer liegen — das prüft die neue `BasicGuard.output_match_crossing` und zieht sie sonst zurück. Die Regel bleibt damit im Guard; `process_output` liefert nur Text, nicht die Fundstellen. **(b) Roher Text im Store:** `stream()` sammelte die Rohtokens, also stand bei `pii_protection: true` die unmaskierte Fassung in `data/conversations.sqlite3` und im JSONL-Mitschnitt, während der Bildschirm maskiert war — über Verlauf, Markdown-Export und JSON-Download kam sie vollständig wieder heraus. Aufgezeichnet wird jetzt, was der Moderator freigibt. **(c) Quadratische Laufzeit:** `process_output` lief pro Token über den gesamten bisherigen Text. Mit der ausgelieferten Config gemessen: 4000 Tokens von **1.605 ms auf 85 ms**, 1000 Tokens von 102 auf 23 ms — und flach statt wachsend, weil nur noch ein Fenster um die Freigabegrenze geprüft wird (`_CONTEXT_WINDOW_CHARS`). **Das Testgerüst war der erste Schritt, nicht das Nebenprodukt:** Maskierung *während* des Streams war komplett ungetestet, `test_default_holdback_keeps_key_material_hidden` deckt nur den Blocklist-Pfad ab, der die Länge nie ändert. Die neue Invariante („gestreamt = am Stück, solange das Muster in den Holdback passt") ist über Text × Häppchengröße × Holdback parametrisiert; dazu eine Schranke gegen den Rückfall in quadratisches Wachstum, die bewusst das Verhältnis statt der absoluten Zeit misst. 50 Fälle fallen mit dem alten Code. **Zwei Assertions mussten dabei entschärft werden, weil sie strenger waren als der Vertrag:** ein Muster, das länger ist als der Holdback, darf seinen Präfix zeigen (steht so im Code), und Text *vor* einem geblockten Secret darf schon draußen sein. | Security/Performance |
| 57 | Review-Befunde Runde 2 (externes Review) | **DONE (2026-07-31): externes Review über das ganze Projekt, dann die sieben Befunde umgesetzt, die chirurgisch und ohne echtes Modell abnehmbar sind; der Rest ist als #58–#64 ticketiert, #14 wurde erweitert und hochgestuft.**

**Zuerst die Selbstkorrektur, weil ein Bericht ohne sie nichts wert ist.** Die Befundliste wurde vor der Umsetzung komplett gegengeprüft, und dabei fiel eigener Pfusch auf. **Zurückgezogen:** „der Guard macht den E-Mail-Adapter unbenutzbar" — schlicht falsch, config.yaml liefert `pii_protection: false` aus, eine Geschäftsmail mit Signatur geht sauber durch; der Beleg war gegen *alle Flags an* gefahren statt gegen die ausgelieferte Config. **Halb zurückgezogen:** der geleakte PII-Präfix beim Streamen steht als Best-effort-Grenze im Code („patterns longer than this window can still leak their prefix") — echt bleibt davon nur der *verschluckte* Modelltext. **Herabgestuft:** die Klartext-PII im Store greift nur bei `pii_protection: true`, ist also latent statt akut. **Übertrieben:** „~14 Gradio-Advisories" waren nach Abgleich mit der real benutzten Komponentenmenge **sechs** (kein Flagging, weil gr.Blocks statt gr.Interface; kein OAuth, kein FileExplorer, kein Dataframe). **Teilweise bekannt:** der Dovecot-Trenner stand längst in #14(a) — neu ist nur seine Folge.

**Umgesetzt.** **(1) Wiki-Proxy:** lief als einthreadiger HTTPServer auf *allen* Interfaces. Vier gleichzeitige Lookups serialisierten exakt (311/617/920/1223 ms bei 300 ms Backend) — die Wiki-Phase des parallelen Broadcasts war damit so seriell wie vor #23; und eine einzige halb geöffnete Verbindung aus dem LAN legte jeden Lookup lahm (Timeout nach 5 s). Jetzt ThreadingHTTPServer auf 127.0.0.1 mit Handler-Timeout, Serveraufbau als build_server() heraus, damit der Test die echte Verdrahtung trifft statt seiner eigenen Nachbildung. **(2) Migrationen:** executescript klammert nicht — scheiterte Anweisung 2 von 3, blieb Anweisung 1 stehen und user_version zurück, der Schritt war *nie wieder* anwendbar, und der Store degradierte bei jedem Start dauerhaft zum NullStore, während die App weiterlief und nichts mehr aufzeichnete. Der Modul-Docstring kündigt FTS5 als Schritt 2 an: die Mine lag, sie war nur nicht getreten. Jetzt BEGIN/COMMIT im Skript, user_version in derselben Transaktion, Rollback mit Schrittnummer. Bewusst weiter executescript statt einer Zerlegung an ";" — ein FTS5-Trigger bringt eigene Semikolons im BEGIN…END-Rumpf mit, der Test enthält deshalb genau so einen. **(3) Verlauf-Ownership:** _history_choices filterte nach Nutzer, die vier Handler dahinter nahmen die Gesprächs-ID ungeprüft entgegen; gr.Dropdown.preprocess reicht den Wert des Clients in Gradio 4.44 durch. Mit zwei angemeldeten Nutzern nachgestellt: „bob" las, exportierte, öffnete und **löschte** alices Gespräch. load()/delete() nehmen jetzt ein optionales user, ein fremdes Gespräch verhält sich wie ein nicht existierendes, und _load_from_store ist der eine Engpass, durch den alle vier Wege müssen. **(4) /ask:** require_access hing nur am /v1-Router — /ask bot dieselbe Fähigkeit auf demselben Port ohne Schlüssel und ohne Limit an (80 schnelle Aufrufe: nur 200er), obwohl der Config-Kommentar ausdrücklich zum Setzen des Schlüssels fürs LAN rät. Die Regel liegt jetzt in check_api_access und gibt einen *Wert* zurück, damit beide Endpunkte sie teilen und trotzdem ihre Fehlerform behalten (/v1 OpenAI-Form, /ask FastAPI-Form); Schlüsselvergleich über hmac.compare_digest, /health bleibt frei. **(5) Gast-Prüfung, dritter Weg:** #55 baute die Regel für Verlauf und Upload — der Ladepfad im Terminal liest dieselbe Datei und prüfte nur den Namen, ein Gast-Gespräch lief dort still unter dem Prompt der echten Persona weiter (im Mutationstest wörtlich sichtbar: „LEAH: Antwort der GAST-Persona."). continuable_persona und GUEST_APP liegen jetzt in ui/continuation.py, und der Test ist über **alle drei** Wege parametrisiert statt einer pro Oberfläche — ein vierter Weg muss dort eine Zeile ergänzen. **(6) Guard sieht den Kontext:** der Guard prüfte ausschließlich die letzte user-Nachricht; Wiki-Snippets und RSS-Meldungen gingen an ihm vorbei — und zwar als system-Nachricht, also mit mehr Gewicht als die Frage des Nutzers, direkt hinter der Anweisung, ausschließlich diesem Kontext zu folgen. Derselbe Satz, den er beim Tippen blockt, kam über eine heruntergeladene ZIM-Datei ungeprüft durch. accepted_context liegt in tinyguard.py, alle sieben Produktivstellen reichen den Guard durch; bewusst nur prompt_injection und wrongdoing verwerfen, nicht pii_detected (ein Artikel darf Adressen enthalten), und ein schlechter Eintrag wirft nicht das ganze Briefing weg. Die Rolle bleibt system — das Verschieben nach user ist #60, weil es ohne Modell nicht abnehmbar ist. **(7) Zirkulärer Import:** "import storage" als erster Projekt-Import scheiterte über core/__init__ → streaming_provider → storage; es fiel nur nie auf, weil jeder bestehende Einstiegspunkt zufällig core zuerst lädt. Re-Exports ersatzlos raus, Test je Paket im frischen Subprozess — im selben Interpreter ist der Zyklus unsichtbar.

**Arbeitsweise:** jeder Fix ist mutationsgeprüft (Fix zurückgenommen → Test fällt), Verlauf-Ownership und Kontext-Filter zusätzlich gegen eine laufende Instanz. Dabei noch ein eigener Fehler, der festgehalten gehört: die erste Ende-zu-Ende-Probe zeigte „bob blockiert" — aus dem falschen Grund. gr.State-Werte kommen serverseitig aus dem StateHolder (`if block.stateful: processed_input.append(state[block._id])`), der Client-Wert wird verworfen; mit frischem session_hash pro Aufruf war user_state immer leer, also scheiterten *beide* Nutzer. Erst mit demo.load im selben session_hash war der Test gültig. 633 → 672 Tests. | Technical foundation |
| 55 | Review-Befunde („Oberfellpfleger 4.0") | **DONE (2026-07-31): kompletter Review mit Reviewer-Brille, dann alles abgearbeitet. **B1 (schwer):** die WebUI ist ein AppFactory-Singleton und bedient alle Browser — Persona, Streamer, beide Kill-Switches und der Self-Talk-Runner hingen aber am Objekt. Im Browser nachgestellt: A wählt LEAH, B danach DORIS, A fragt → die Nachricht landet in DORIS' Gespräch, LEAHs bleibt leer. Der Zustand liegt jetzt in `SessionContext` (`ui/session.py`) und reist als `gr.State` durch die Handler; Gradio kopiert den Default-Wert pro Sitzung (`SessionState.__getitem__` macht ein `deepcopy`), deshalb reicht In-place-Änderung ohne Output. Die Attribute sind **ersatzlos** weg — eine übersehene Stelle ist damit ein AttributeError im Test statt stiller Vermischung. **T1 (bester Schnitt):** `webui_layout.py` lag bei 2 % Deckung, `_bind_events` bei 0 %. `tests/test_web_ui_wiring.py` baut die echte Oberfläche in-process (nur `_start_server` abgefangen) und prüft alle 31 Events: Komponenten vorhanden, Handler-Signatur zur `inputs=`-Liste passend, `cancels` nur auf gequeuete Events (der Fehler aus #35 verhinderte den App-Start komplett), Generator-Handler gequeuet, `PERSONA_OUTPUT_KEYS` deckungsgleich mit `_reset_updates`. Der Test lieferte gleich einen echten Fund: liegt eine optionale Abhängigkeit als Stub ohne `__spec__` in `sys.modules`, wirft `importlib.util.find_spec` ValueError — die WebUI startete dann gar nicht, statt den Vorlesen-Button auszublenden (neu: `core.utils.module_available`). **B2:** ein Gast namens „Leah" wurde im Verlauf still als die echte LEAH fortgesetzt (Auflösung über `ref.persona.lower()`); jetzt eigenes `app` (`web-guest`) **und** exakter Namensvergleich, Letzterer fängt auch Altzeilen. **B3:** drei `NamedTemporaryFile(delete=False)` wurden nie gelöscht — jetzt je Sitzung und Art die vorherige, plus `atexit`-Aufräumen des Verzeichnisses; das betrifft die Originale, Gradios eigene Cache-Kopie verwaltet Gradio. **R2:** `lookup_wiki_snippet` stand an sechs Stellen mit denselben acht Argumenten, dieselben fünf `wiki_*`-Attribute in drei Klassen → `WikiLookup`, einmal von der Factory gebaut. **R3:** Guard-Eingangscheck war in `stream()` und `respond_one_shot()` verdoppelt. **R1:** die reinen Formatierer nach `ui/webui_format.py`, die Ausgabe-Key-Listen zum Layout (2225 → 2096 Zeilen, 71 → 64 Methoden — weniger als das Verschobene, weil die Sitzungstrennung Zeilen hinzufügt; eine Zerlegung in Controller pro Ansicht bleibt offen). **T2:** `wikipedia_proxy.do_GET` von 20 % auf 61 %, indem die Auswertung als `handle_lookup(path, host) → ProxyResponse` aus dem Socket-Code heraus ist. **Toter Code:** `BasicGuard.reset_session()` (unerreichbar — die Factory baut je Auswahl einen neuen Streamer), `SpacyKeywordFinder.find_top_keyword()` (nur Tests), `SqliteStore.close()` wird jetzt per `atexit` gerufen. **Kleinigkeiten:** `storage.history_limit` statt hart 50; das `finally` in `do_GET` loggte eine bei frühem Fehler ungebundene Variable. **Ein Befund wurde beim Nachsehen entkräftet:** die Konsolenwarnung „Too many arguments provided for the endpoint" kommt aus Gradios Frontend und trifft **jedes** Event mit einem `gr.State`-Input (`skip_api = True`) — kein Fehler im Projekt, jetzt als Stolperfalle dokumentiert. 668 Tests (+13 gegenüber dem Stand davor bei gleichzeitig 41 gelöschten). **Zweite Runde nach einem Review des eigenen PRs:** der Gast-Fix schloss nur den Verlauf — über „Konversation herunterladen" → Upload lief ein Gast weiter als die echte Persona, dort sogar *fortsetzbar*. Ursache war, dass es die Prüfung zweimal gab; jetzt benutzt auch `_on_load_conversation` `_continuable_persona` (auf Primitiven statt auf `ConversationRef`, damit beide Wege dieselbe Regel teilen), und `_build_meta` reicht das `app` durch, damit die Markierung den Export überlebt. Außerdem prüfte der Test zur Sitzungstrennung `copy.deepcopy` auf der eigenen Dataclass — also die eigene Annahme statt Gradios Verhalten; der neue Test geht über den echten `StateHolder`. Beide neuen Tests sind mutationsgeprüft (mit der alten Auflösung bzw. ohne Gradios `deepcopy` fallen sie). **Dritter Durchgang:** einmal Entwarnung — der neue exakte Namensvergleich ist keine Regression, denn alle Erzeuger schreiben den kanonischen Namen und der Terminal-Lader prüft seit jeher exakt. Und ein Fund, der älter ist als dieser PR (seit #54): `_on_load_conversation` eröffnete kein Gespräch in der Ablage, `conversation_state` blieb leer und `SqliteStore.append` steigt bei leerer ID aus — wer eine JSON hochlud und weiterschrieb, schrieb still ins Nichts. Jetzt eröffnet der Upload-Pfad ein Gespräch mit eigenem `app` (`web-import`), wie es das Terminal nach dem Laden längst tat. 676 Tests.** | Technical foundation |
| 51 | WebUI: ~4 s Verzug bis der erste Token im Browser erscheint | **DONE (2026-07-30) — Ursache gefunden, und es war nicht das Frontend.** Messkette im Browser mit instrumentierten SSE-Frames (`/queue/data`): eine nackte Gradio-App zeigt den ersten Token 0,95 s nach Klick, die Projekt-App 4,13 s. Der SSE-Frame **verlässt den Server** erst bei +4,09 s, obwohl das Modell schon bei +0,34 s liefert; gerendert wird danach in 40 ms. Es lag also weder an Gradio noch am Rendern, sondern am eigenen Code: `_StreamModerator` hält `_STREAM_HOLDBACK_CHARS = 96` Zeichen zurück, damit ein PII-/Secret-Muster nicht über eine Token-Grenze rutscht — bei 6-Zeichen-Tokens und 4 Tokens/s sind das exakt die gemessenen 4 s. Gegenprobe: dieselbe App mit `holdback = 0` zeigt den Token nach 0,39 s. **Das ist kein Bug, sondern der Preis einer bewussten Sicherheitsentscheidung** — nur hatte sie niemand mit der wahrgenommenen Latenz verknüpft. Umgesetzt: (a) neuer Schalter `security.stream_holdback_chars` mit der Abwägung dokumentiert in beiden ReadMes, Default seit dem Folge-Commit **32** (nicht rund gewählt: das längste Blocklist-Muster schlägt erst nach Label + 30 Zeichen an, darunter rutscht Schlüsselmaterial mit durch) — 96 bleibt die Empfehlung für geteilte Instanzen; (b) sind beide Ausgangs-Checks (`pii_protection`, `output_blocklist`) aus, entfällt der Holdback automatisch, weil er dann nur Latenz ohne Schutz kostet; (c) CLAUDE.md-Sektion mit der Messtabelle und dem Hinweis, beim Suchen nicht im Frontend anzufangen. 6 Tests. **Empfehlung für den Betrieb:** wer lokal und für sich nutzt, kann auf 0–32 runter und gewinnt ~3,5 s gefühlte Antwortzeit.** | Performance |
| 50 | Guard: Braces-Injection nach Leerzeichen | **DONE (2026-07-30): Die alte Regel `\b%?\{.*?\}%?` verlangte durch das führende `\b` ein Wortzeichen direkt vor der Klammer und griff deshalb fast nie — gemessen **1 von 6** naheliegenden Varianten (`… mit x{system_prompt}` ja, `… mit {system_prompt}`, `{instructions}`, `%{prompt}%`, `{rules}`, `{SYSTEM PROMPT}` nein). Das `\b` einfach zu entfernen war keine Option: dann blockiert jede Code-Frage mit geschweiften Klammern. Jetzt zielt die Regel auf die Platzhalter-*Namen* (`_INJECTION_PLACEHOLDER`: system_prompt, instructions, prompt, rules, persona, jailbreak, developer_mode, dan) statt auf die Klammern — **6/6 gefangen, 0 von 7 harmlosen Code-Fragen blockiert** (`if (x) { return; }`, JSON, f-String, CSS, `${HOME}`, `dict = {}`, `const a={b:1}`). `known_gap` ist aus `evals/guard_redteam.yaml` raus, dafür 4 neue Angriffs- und 4 neue harmlose Gegenfälle (Korpus jetzt 33 geprüfte Fälle statt 24). Gegenprobe in `tests/test_security.py` als zwei parametrisierte Tests. Nebenbei: der Test, der die dokumentierte Lücke im Report prüfte, hing am konkreten Fall — jetzt prüft er (a) dass der Repo-Korpus gapfrei bleibt und (b) den known_gap-Mechanismus an einem synthetischen Korpus, damit er das Schließen einer Lücke überlebt.** | Security |
| 34 | Audio plattformübergreifend | **DONE (2026-07-30): `audio_player.play_wav` dispatcht jetzt nach Plattform — Windows weiter über `winsound` (stdlib), Linux über `paplay` → `aplay` → `ffplay` (erster Treffer gewinnt, deckt PulseAudio/PipeWire, reines ALSA und ffmpeg-only-Kisten ab), macOS über `afplay`. Keine neue Dependency. Fehlt jeder Player, wird still übersprungen und nur geloggt — Audio ist hier Beigabe, ein fehlender Player darf keinen Terminal-Chat abschießen. Rückgabewert sagt, ob die Wiedergabe gestartet ist; der `platform`-Parameter macht den Dispatch ohne die jeweilige Plattform testbar (12 Tests, inkl. Windows-Pfad mit gefaktem winsound). Nebenbei sechs Doku-Stellen korrigiert, die noch „nur unter Windows" behaupteten.** | Technology |
| 45 | CI-Ausbau | **DONE (2026-07-30, teilweise — Rest bewusst abgegrenzt): `--cov` lief schon; neu ist die OS-Matrix mit `windows-latest` neben `ubuntu-latest` (`fail-fast: false`, damit ein Windows-Fehler das Linux-Ergebnis nicht verschluckt) — das war der eigentliche Wert des Tickets, weil das Projekt Windows-primär läuft und die CI bisher nur Linux sah. Dazu ein eigener Job `test-with-spacy`: lädt `de_core_news_lg` (~570 MB) mit `actions/cache` gegen einen versionierten Key und fährt gezielt `test_spacy_keywords.py` + `test_wiki.py` — die liefen bisher in der CI ausschließlich als Skips. Eigener Job, damit ein Fehler dort klar zuzuordnen ist. Coverage steht als Zahl in der Job-Summary; **auf den Badge bewusst verzichtet**, weil er einen externen Dienst mit Account und Token bräuchte — das ist eine Entscheidung für den Projektinhaber, nicht für einen Commit. **mypy bewusst nicht angefasst:** ein erster Lauf über `src/core` produziert erfahrungsgemäß eine Menge Findings; entweder wird der Job dauerhaft gelb (wertlos) oder es braucht eine eigene Aufräumrunde. Als #52 abgetrennt.** | Technical foundation |
| 37 | OpenAI-kompatible API | **DONE (2026-07-30): `/v1/models` und `/v1/chat/completions` in `src/api/openai_compat.py`, Router immer gemountet, Feature-Flag pro Request (`api.openai_compatible.enabled`, aus = 404). `model` ist der Persona-Name, case-insensitive; `/v1/models` listet daher Personas, nicht LLMs. Streaming als SSE mit Rollen-Chunk zuerst und `data: [DONE]` zum Schluss; nicht-streamend inklusive `usage` (Schätzung über `approx_token_count`). Neuer Provider-Pfad `AiApiProvider.stream_messages`: reicht die Client-History unverändert durch (OpenAI-Semantik, Karl/Heuristik bewusst außen vor), injiziert Wiki-Kontext unmittelbar vor dem letzten User-Turn und nutzt denselben Streamer wie die UI — Guard, Wiki und Gesprächslog kommen dadurch gratis mit. `temperature`/`top_p`/`max_tokens` werden angenommen und ignoriert, weil Sampling zur Persona gehört. Optionaler Bearer-Key (`env:`-Konvention wie beim Mail-Adapter) und Fixed-Window-Rate-Limit pro Client — damit ist die Definition of Done erfüllt. **Wichtigster Fund beim Verifizieren mit dem echten openai-SDK:** FastAPIs `HTTPException(detail=…)` serialisiert zu `{"detail": {"error": …}}`, eine Ebene zu tief — Clients lesen `error.message` und finden nichts. Deshalb eigene `OpenAIError` plus Exception-Handler, und `RequestValidationError` wird unter `/v1` auf 400 + OpenAI-Form gemappt (`/ask` behält FastAPIs Standardform). Ende-zu-Ende gegen das offizielle SDK über echtes HTTP geprüft: 401 bei falschem Key, 4 Personas in `models.list()`, nicht-streamende Antwort mit usage, Streaming mit Rollen-Chunk, 404 mit `model_not_found`, 400 bei ungültigem Request. `openai` bewusst **nicht** als Dependency aufgenommen, nur zum Verifizieren installiert. 29 Tests.** | Integration |
| 35 | Stream-Steuerung: Stop + Regenerate | **DONE (2026-07-30): Stop-Button (ersetzt „Senden" während des Streams) und „Nochmal"-Button im Persona-Chat. Kill-Switch-Muster von `_ask_all_stop` übernommen: `WebUI._stream_stop` (`threading.Event`) wird vom Stop-Event gesetzt, der Stream-Generator bricht geordnet ab und **behält die Teilantwort** (Suffix aus `web_stream_stopped_suffix`) — sie ist der Grund, warum man abbricht. Der Token-Iterator wird explizit geschlossen, damit das `finally` im Streaming-Provider den Ollama-Stream deterministisch beendet statt irgendwann per GC. Identitätsprüfung auf das Event, damit ein alter Generator nicht auf den Kill-Switch eines neueren reagiert. Gilt für Einzelchat, Briefing und Self-Talk (dort zwischen den Turns — `run_turn()` liefert die Antwort in einem Zug, es gibt keinen Token-Strom zum Abbrechen). Regenerate verwirft die letzte Antwort in Anzeige und LLM-Verlauf und streamt denselben Kontext erneut; Wiki-/Briefing-Hints bleiben stehen, weil sie vor der Antwort liegen. Varianz kommt allein aus der Persona-Temperatur. **Wichtiger Umweg beim Bauen:** der Button-Tausch lief zuerst als eigene `.then()`-Events vor und nach dem Stream — das kostete gemessen ~3,5 s bis zum ersten Token (Queue-Roundtrip) und hätte #17 zunichte gemacht; jetzt reisen die Button-Updates in denselben Yields mit (`_with_stream_controls`), Stop erscheint nach 0,16 s. Zweiter Fund: `cancels` kann nur gequeuete Events abbrechen — zeigt die Liste auf ein `queue=False`-Event, verweigert Gradio den Start komplett. Im Browser verifiziert (Playwright, langsam getakteter Dummy-Stream): Teilantwort bleibt, Stream endet wirklich, Buttons tauschen korrekt, Regenerate ersetzt genau eine Antwort. 16 Tests. Bonus-Variante „andere Persona antwortet stattdessen" bewusst nicht gebaut. Nebenbei gemessen und als #51 eingetragen: bis der Token im Browser sichtbar ist, vergehen ~4 s — Frontend-seitig, nicht von diesem Ticket verursacht.** | UX |
| 41 | Eval-Suite (Charakter, Qualität, Guard) | **DONE (2026-07-30): Korpora als YAML unter `evals/` (goldene Fragen pro Persona, Verhaltensfälle für die drei Zeitstempel aus #19, Karl-Zusammenfassungen aus #12, Guard-Red-Team), Code in `src/evals/`, CLI `python scripts/run_evals.py` (+ `make evals` für den modellfreien Teil). Zwei Sorten Erwartung: `checks` (Regex/Länge, deterministisch, mit Laufzeit-Platzhaltern wie `{today_de}`) und `expect_traits` (LLM-as-judge, 1–5, 4+ gilt als erfüllt, 3 bewusst nicht). Judge läuft mit temperature 0; unlesbare Judge-Antworten werden als `unscored` geführt, nie als bestanden. Report als `report.md` (lesbar, zeigt die Antworten der Fehlschläge) und `report.csv` (stabile Spalten → zwei Läufe vergleichbar, das Artefakt für #7). Exit-Code 1 bei Fehlschlag. Der Guard-Teil braucht kein Modell und läuft als parametrisierter Test in der CI mit (`tests/test_guard_redteam.py`, 29 Fälle inkl. harmloser Gegenbeispiele). Loader ist absichtlich streng: unbekannte Keys, kaputte Regexe, doppelte IDs und erwartungslose Fälle fliegen beim Laden raus. Neu im Korpus-Format: `known_gap` für dokumentierte Guard-Schwächen (Meldung ohne Fehlschlag, Gegentest erinnert ans Entfernen) und „übersprungen", wenn der zuständige Schutz per Config aus ist — dadurch trennt der Report „Guard kaputt" von „du hast das abgeschaltet". 89 Tests offline, 2 live hinter `@pytest.mark.ollama`. Dabei gefunden: Braces-Injection-Lücke → #50. Offen: der erste echte Baseline-Lauf → #41a.** | Quality |
| 6 | Model selection in the UI | **DONE (2026-07-07, bewusst verengt auf Nutzerwunsch: ein globaler Session-Override statt „pro Persona", dezent als Profi-Option): zugeklapptes „Erweitert"-Accordion am WebUI-Startbildschirm mit `gr.Dropdown` der installierten Ollama-Modelle (`fetch_model_names` via `/api/tags`, jetzt public in `system_checks.py`), Default `core.model_name` vorausgewählt. Wechsel = `Config.override("core", …)` (session-only, `config.yaml` unangetastet) + Rebuild des aktiven Streamers; Ask-All/Self-Talk/Karl/Cutoff-Zeile folgen automatisch, weil sie frisch aus der Config lesen. Greeting liest das Modell jetzt live statt beim Start eingefroren. Ollama down/Dummy-Backend → Dropdown inert mit nur dem Default. 10 Tests. Live-Test am echten Ollama steht aus.** | UX/Technology |
| 13 | STT MVP | **DONE (2026-07-08, Scope: nur Einzelchat der WebUI): Mikrofon (`gr.Audio`, `stop_recording`) neben dem Eingabefeld, erscheint nur wenn `stt.enabled` UND faster-whisper installiert (`is_stt_available`, sonst unsichtbar + INFO-Log). Transkription lokal via neues Modul `src/stt/whisper_stt.py` (Lazy-Import + Modell-Cache pro model/device/compute_type, Muster wie Piper-TTS: optionale Lib, nicht in `requirements.txt`, Setup in `src/stt/ReadMe.md`). Transkript wird ans Eingabefeld angehängt — editierbar, kein Auto-Send. Neue Config-Sektion `stt:`, `mic_audio` als 29. Slot der `PERSONA_OUTPUT_KEYS`. 13 Tests. Offen: Live-Test mit echtem Mikro/GPU; kein Streaming-STT, kein Ask-All/Terminal.** | Cool feature |
| 15 | Daily briefing (RSS-Teil) | **DONE (RSS-MVP, 2026-07-09): neues Modul `src/briefing/feeds.py` spiegelt die Wiki-Pipeline — `fetch_briefing_items` (RSS 2.0 + Atom via stdlib `xml.etree`, keine neue Dependency, pro Feed fehlertolerant) und `inject_briefing_context` (Guardrail + System-Message pro Meldung). Auslösung per Config (`briefing.enabled` + `feeds`, Default tagesschau + heise): WebUI-Button „Briefing 📰" (30. Slot) und Terminal-`/briefing`. Netzzugriff nur auf Klick, kein Hintergrund-Fetch. 11 Tests. Offen: IoT-Teil (bewusst nicht angefasst), Live-Test mit echten Feeds (Sandbox-Proxy blockte ausgehende Fetches — Degradations-Pfad dabei bestätigt).** | Cool feature |
| 33 | TTS im WebUI | **DONE (2026-07-11): Button „Vorlesen 🔊" liest die letzte Antwort mit der Piper-Persona-Stimme (`tts.voices`) vor — Quelle ist die letzte assistant-Message aus `history_state` (nicht der Chatbot, dort stecken Wiki-/Briefing-Hints), synthetisiert via `create_wav` in ein Tempfile, abgespielt über verstecktes `gr.Audio(autoplay=True)` → plattformunabhängig, kein winsound. Nur bei `tts.enabled` + `tts.features.web_read_aloud` (Default on) + installiertem piper-tts. Nebenbei: `_voice_cache` in `piper_tts.py` — `PiperVoice.load` lief bisher bei jedem Aufruf, jetzt einmal pro Modellpfad (Terminal profitiert mit). `read_aloud_btn`/`tts_audio` als Slots 31/32. 7 Tests. Offen: Live-Test mit echten Stimmen; bewusst kein Auto-Vorlesen und kein Per-Message-Button (kann Gradio 4.44 nicht).** | Cool feature |
| 40 | Feedback-Daumen (V1) | **DONE (2026-07-30): `chatbot.like()` in `web_ui.py:_bind_events` — das Binding aktiviert die Daumen automatisch, `webui_layout.py` blieb unangetastet. `_on_chat_like` schreibt append-only nach `logs/feedback_votes.jsonl` (ts, app, persona, model, vote, question, answer, index); Persona/Modell aus `meta_state`, nicht aus `cfg.core`. `_find_question_for_row` läuft rückwärts zur nächsten Nutzerzeile und deckt Live-Chat, geladene Gespräche und Self-Talk ab. Schreibfehler werden geloggt und geschluckt, Modul-Lock gegen parallele Sessions. Live am Dummy-Backend im Browser verifiziert — dabei gefunden: Gradio 4.44 rendert die Daumen auch auf der Nutzerzeile, solche Votes (Spalte 0) werden verworfen, weil `question == answer` kein Trainingssignal ist. 8 Tests.** Blind-Ranking/ELO ausgelagert nach #40b. | Quality/Data |
| 29 | Spaceship-Crew sichtbar machen | **DONE (2026-07-30): neues `--list-ensembles` in `launch.py` (reiner YAML-Scan über `ensembles/**/personas_base.yaml`, Early-Exit vor dem `-e`-Zwang, keine schweren Imports — Personas mit `(featured)`-Marker, vorhandene Sprachen, Exit 1 wenn nichts gefunden). README-Sektion „🎭 Mitgelieferte Ensembles" mit Tabelle und frischem Screenshot (`docs/screenshot_spaceship_crew.png`, per Playwright gegen das Dummy-Backend aufgenommen), neue Sektion „Persona-Ensembles" in `docs/{de,en}/Features.md`, „Ensembles auflisten" in beiden ReadMes. Entscheidung: bleibt unter `examples/` — der Pfad-Name funktioniert Ende-zu-Ende, ein Verschieben hätte nur 12 Binärdateien umgeschichtet. Nebenbei zwei Doku-Bugs gefixt: DE-ReadMe nannte `examples\spaceship_crew` mit Backslash, EN-ReadMe empfahl unter Windows sogar aktiv den Backslash — beides bricht die Avatar-URLs der Web-UI. 4 Tests.** | Docs/UX |
| 18 | Wrongdoing guardrail (violence/weaponization) | **DONE: `wrongdoing_protection` in `tinyguard.py` (EN/DE verb+objekt-Patterns, Session-Lock via `_wrongdoing_lock_remaining`; das `reset_session()` von damals ist mit #55 als unerreichbar entfallen), Locale-Template `security_wrongdoing`, Config-Flag, 17 Unit-Tests in `tests/test_wrongdoing_guard.py`. Default on.** | Security/Robustness |
| 19 | Drei-Zeitstempel-Transparenz | **DONE: `_system_prompt_with_date` (src/core/utils.py) baut einen beschrifteten Drei-Stempel-Block — Datum aus Systemuhr, Cutoff aus Per-Modell-Map `core.knowledge_cutoffs`, Wiki-Stand aus `zim_prefix` (Regex) bzw. live/aus. Fehlende Werte ehrlich als 'unbekannt' benannt, Guidance-Satz gegen Verwechslung. 7 Locale-Keys (de/en), 17 Tests inkl. End-to-End über Factory.** Restpunkt „Verhaltensbeweis am echten LLM" → #41. | Quality/Correctness |
| 2 | Web-UI "New conversation" in stream | **DONE: "New conversation" (Chat/Self-Talk) und Ask-All-Reset brechen laufende Streams jetzt aktiv ab (`cancels=[…]` in `web_ui.py:_bind_events`); der geschlossene Generator beendet über das `finally` in `YulYenStreamingProvider.stream` auch den Ollama-Stream.** | Bugfix |
| 9 | Ask-all (broadcast): WebUI Wiki + Streaming | **DONE: Wiki-Kontext im Broadcast — Lookup läuft einmal pro Frage (WebUI `_on_submit_ask_all` + Terminal `_run_ask_all_flow`), Snippets gehen via `context_messages` durch `orchestrator.py` an alle Personas, Hints erscheinen im Status/Terminal. Live-Streaming im WebUI war bereits umgesetzt.** | Orchestration |
| 20 | Ask-all results table polish | **DONE (2026-07-04, anders als geplant): editierbare `gr.Dataframe` komplett durch live gestreamte `gr.Markdown`-Sektionen ersetzt — die Dataframe-Komponente verliert in Gradio 4.44 Streaming-Updates aus Generatoren (per Minimal-Repro bestätigt). Read-only, Markdown sauber gerendert, keine Scrollbalken.** | UX/Technology |
| 5 | Health checks/monitoring | **DONE: `/healthz` (FastAPI) liefert aggregierten Status (ok/degraded/error, HTTP 503 bei kritischem Ausfall) aus gemeinsamem `core/system_checks.py` (Ollama erreichbar, Modell gepullt, spaCy, Kiwix, VRAM via nvidia-smi). `/health` bleibt als günstiger Liveness-Stub.** | Technical foundation |
| 21 | Setup-Doktor / Preflight-Check | **DONE: `python src/launch.py --doctor` nutzt dasselbe `core/system_checks.py` wie #5, farbiger Report (colorama), Exit-Code 1 bei kritischem Ausfall. Schwere Imports (gradio/uvicorn) lazy gemacht, damit der Doktor auch bei kaputtem UI-Stack läuft.** | Technical foundation |
| 22 | Kiwix/ZIM aktualisieren + Anleitung | **DONE (2026-07-05): `wikipedia_de_all_nopic_2026-01.zim` (13,6 GB) geholt, Config + `start-wiki-offline.bat` umgestellt, live verifiziert. Anleitung: `docs/{de,en}/Kiwix_Setup.md`. Alte 2025-06-ZIM (15,7 GB) liegt noch in `C:\wikipedia-de-offline\` — nach Abnahme löschen.** | Integration/Docs |
| 23 | Ask-All: Broadcast parallelisieren | **DONE (2026-07-05): `iter_broadcast_events_parallel` in `orchestrator.py` — alle Personas streamen gleichzeitig (Worker-Thread + Event-Queue pro Persona). WebUI per Default; Fallback `ui.experimental.broadcast_parallel: false`. Wichtigster Fund: Gradio `cancels` schließt Generatoren NICHT → expliziter Kill-Switch `WebUI._ask_all_stop`. Echter Speedup braucht `OLLAMA_NUM_PARALLEL` ≥ Persona-Zahl.** | Orchestration/UX |
| 17 | Faster first token | **DONE (2026-07-07, schlanke Variante): (1) Warm-up repariert — einmaliger Hintergrund-Preload beim Start (`AppFactory.warm_up_model()`) mit echtem `num_ctx` + `keep_alive`. (2) Neu `core.keep_alive` (Default 600, `-1` = für immer). (3) Stream-Drossel im WebUI: Updates koalesziert auf ~10/s. (4) SHA-256-Payload-Hash nur bei DEBUG. Bewusst NICHT: Prompt-Diät.** Restpunkt Latenz-Verifikation → #42. **Nachtrag (#51):** die Messung erfolgte backendseitig und sieht den größten Anteil der *wahrgenommenen* Latenz nicht — der Guard-Holdback verzögerte die Anzeige um weitere ~4 s. | Performance |
| 12 | Karl (context summarizer) | **DONE (MVP): `KarlSummarizer` in `src/core/context_summarizer.py`, opt-in via `context_management.strategy: "karl"` (Standard bleibt `heuristic`), Fallback auf Heuristik bei Fehlern, Tests in `tests/test_context_summarizer.py`.** Restpunkt Qualitäts-Eval → #41. | Technical foundation |
| 32 | Wiki-Quellen-Transparenz | **DONE (2026-07-30, bewusst über das Ticket hinaus): zugeklapptes `gr.Accordion` „Quellen 📚" unter dem Chat zeigt nicht nur Titel und Link, sondern den *injizierten Snippet-Text selbst* plus die Zeichenzahl — „1200 von 9800 Zeichen injiziert (gekürzt)" bzw. „51 Zeichen (vollständig)". Das ist der eigentliche Punkt: erst daran ist erkennbar, ob eine Antwort auf echtem Kontext beruht und wie viel des Artikels das Modell nie gesehen hat. Dafür musste die Datenkette nachgezogen werden: `lookup_wiki_snippet` warf `link` und `source` aus der Proxy-Antwort weg und gab nackte `(topic, snippet)`-Tupel zurück; jetzt ein `WikiSnippet`-Dataclass mit `link`/`source`/`full_length` und `truncated`. Die Originallänge kannte niemand mehr, weil der Proxy schon kürzt — deshalb liefert er sie als neues Feld `full_length` mit (Kürzungslogik dafür als testbares `_apply_limit` herausgezogen); fehlt das Feld, gilt der Snippet als vollständig statt fälschlich als gekürzt. Die Quellen reisen in denselben Yields wie die Tokens mit (`STREAM_OUTPUT_KEYS`) — ein vorgeschaltetes `.then()`-Event hätte nach der Messung aus #35 ~3,5 s bis zum ersten Token gekostet. Bei einer neuen Frage verschwinden die alten Quellen sofort, bei „Nochmal" bleiben sie stehen (gleicher Kontext). Im Browser gegen einen Fake-Proxy verifiziert (Playwright, echtes spaCy): Link, Quelle, Zeichenzahl und Snippet-Text stehen da, gekürzt und vollständig beide geprüft. 16 Tests. **Nicht in V1:** Ask-All (eigene Ansicht ohne Accordion) und Terminal-UI → #32a.** | UX/Trust |
| 32a | Quellen-Anzeige für Ask-All und Terminal | **DONE (2026-07-30): Nachzug zu #32. Ask-All bekommt ein eigenes, zugeklapptes Accordion *innerhalb* der Ask-All-Gruppe — das Einzelchat-Accordion liegt außerhalb und stünde in dieser Ansicht über dem ganzen Block. Der Lookup lief dort ohnehin schon einmal für alle Personas; die Quellen reisen jetzt wie `wiki_status` durch jeden Yield mit (`_ask_all_state` von 5 auf 7 Slots, neue Konstante `ASK_ALL_OUTPUT_KEYS`, an der auch die Tests hängen statt an festen Indizes). Im Terminal ein Kommando `/quellen` (auch `/sources`, weil Kommandos nicht übersetzt sind): zeigt Titel, Herkunft, Zeichenzahl, Link und den Ausschnitt **ungekürzt** — dass der Text vollständig dasteht, ist der Grund für das Kommando. `TerminalUI.last_wiki_snippets` wird aus beiden Lookup-Pfaden gefüllt (Chat-Schleife und Ask-All-Flow) und bei neuer Unterhaltung bzw. geladenem Gespräch geleert. Die Meta-Zeile („Offline-Archiv · 1200 von 9800 Zeichen injiziert (gekürzt)") ist als `format_snippet_meta` nach `wiki/lookup.py` gewandert, damit die Auswahl der Locale-Keys nur an einer Stelle steht und Browser und Terminal wortgleich dasselbe zeigen. Ask-All-Teil im Browser gegen den Fake-Proxy verifiziert (Playwright), Terminal-Teil über capsys. 10 Tests.** | UX/Trust |
| 52 | mypy für src/core | **DONE (2026-07-30): 16 Findings gemessen, alle abgearbeitet, danach als blockierender CI-Job `Typen (mypy)` eingehängt (`mypy==1.10.1` gepinnt wie Black/Ruff, Konfiguration in `pyproject.toml` statt einer eigenen `mypy.ini` — das Projekt hält Black/Ruff/pytest schon dort). `follow_imports = silent`: der Rest des Projekts wird für Signaturen mitgelesen, aber nicht gemeldet — sonst hätte man alles auf einmal aufräumen müssen. Stub-lose Bibliotheken einzeln ausgenommen statt global, damit ein neuer Import nicht stillschweigend ungeprüft durchrutscht. **Der eigentliche Fund war ein echter latenter Bug:** `personas.get_options()` liefert `None`, wenn eine Persona keine `llm_options` hat, und genau das wurde als `persona_options` in den Streamer gereicht — aufgefallen wäre es erst mitten im Gespräch beim ersten Kontext-Check (`.get("num_ctx")` auf None). Regressionstest dafür. Zwei weitere Annotationen waren schlicht falsch und damit irreführend: `resolve_spacy_model(cfg: dict)` nimmt in Wahrheit eine `Config`, `check_kiwix_reachable(host: str)` behandelt einen fehlenden Host selbst. Strukturell: die YAML-Sektionen landen per `setattr` auf der Config, ein Typprüfer sah sie nie — jetzt als reine Annotationen deklariert (`core`, `wiki`, `ui`, …), zur Laufzeit ändert sich nichts. `make types` als Kurzform. Erweiterung Modul für Modul, nicht per Big Bang.** | Technical foundation |
| 36 | WebUI-Politur | **DONE (2026-07-30): alle vier Happen. (a) Copy-Button am Chatbot — Gradio kann das seit jeher, es war nur nie eingeschaltet. (b) Dark Mode über `?__theme=dark`; **vor dem Bauen im Browser geprüft**, dass Gradio 4.44 den Parameter wirklich auswertet (`body.class` wird `dark`, `--background-fill-primary` springt auf `#0b0f19`) — ein Toggle ohne Reload gibt es nicht, deshalb zwei schlichte Links statt eines JS-Handlers. Dabei aufgefallen: zwei Rahmenfarben im projekteigenen CSS waren auf `#e3e7ed` festgenagelt und wären im dunklen Theme hell geblieben. (c)+(d) Statuszeile unter dem Chat mit Kontext-Balken und Tempo: `Kontext █░░░ 424 / 8.192 Token (5 %) · 24,0 Tok/s · erster Token nach 1,9 s`. Beide Zahlen kannte das Projekt längst — `context_near_limit` rechnet mit demselben Füllstand, `stream()` maß `t_first_ms`/`t_total_ms` und schrieb sie nur ins Logfile; jetzt eine `StreamStats`-Dataclass, die der Provider auf sich selbst ablegt (kein neuer Rückgabewert, keine Signaturänderung). Ab der 75-%-Schwelle (`context_utils.threshold`) wird die Zeile fett — genau der Punkt, ab dem die Kompression greift, sie erklärt also die „Kaffee"-Meldung, bevor sie kommt. Die Statuszeile reist in denselben Yields mit, aber erst im Schluss-Yield mit Wert. Im Browser verifiziert. 8 Tests.** | UX |
| 43 | Config-/Ensemble-Validierung | **DONE (2026-07-30): neues `src/config/schema.py` mit pydantic-Modellen für `config.yaml` und die Ensemble-Dateien. **Zwei Härtegrade, das ist der Kern:** beim normalen Start nur `logging.warning` — ein laufendes Setup darf nicht an einem Schema scheitern, das die persönliche `config.local.yaml` nie gesehen hat; `--doctor` und `/healthz` melden denselben Befund hart als `CheckResult("config")`, dort *will* man Strenge. Unbekannte Keys sind nie ein Fehler, sondern ein Tippfehler-Hinweis (`extra=allow` plus eigener Abgleich statt `extra=forbid`, sonst blockiert jede neue Sektion sofort alles). Geprüft wird, was erfahrungsgemäß schiefgeht: Pflichtfelder, erlaubte Werte statt beliebiger Strings (`core.backend`, `wiki.mode`, `ui.type`, `context_management.strategy`), `temperature` in [0, 2], `num_ctx` > 0 — und beim Ensemble der Abgleich Basis ↔ Locale in **beide** Richtungen: eine nur lokalisierte Persona wird nie geladen und fiel bisher niemandem auf. Dazu der VRAM-Hinweis aus dem gestrichenen Auto-Modellwahl-Punkt: `check_model_fits_vram` vergleicht die Modellgröße aus `/api/tags` mit dem freien VRAM („braucht ~4,7 GB, frei sind 3,1 GB"), rein additiv — ohne Ollama oder GPU bleibt es bei INFO. Nebenbei die doppelte `nvidia-smi`-Logik in `_nvidia_memory` zusammengeführt. 18 Tests. **Offen:** der VRAM-Abgleich ist nur gegen eine gefälschte `/api/tags`-Antwort geprüft, nicht an echter Hardware.** | Technical foundation |
| 53 | Identitäts-Naht für die Web-UI | **DONE (2026-07-31): neues `src/auth/` mit `AuthProvider`-Protokoll und drei Implementierungen. `DisabledAuth` ist Default und verhält sich **exakt wie vorher** — wer allein am eigenen Rechner arbeitet, merkt vom Ticket nichts. `LocalUsersAuth` nimmt Nutzer aus `ui.web.auth.users` mit der `env:`-Konvention; Nutzer ohne auflösbares Passwort werden verworfen **und** gemeldet, und ohne einen einzigen gültigen Nutzer bleibt die App offen statt zugesperrt und rätselhaft. `HeaderAuth` liest die Identität aus dem Header eines vorgeschalteten Proxys — **das ist der Keycloak-Pfad**, denn Gradios `auth=`-Callable bekommt nur Name und Passwort, kann also kein OIDC (kein Redirect, keine Token-Validierung); der realistische Weg ist oauth2-proxy oder Authelia davor. **Der eigentliche Gewinn ist die Naht:** `user` steht jetzt in *jeder Zeile* des Gesprächslogs (`YulYenStreamingProvider.set_user`) und in jeder Feedback-Vote — genau die zwei Stellen, die #25, #49, #40b und #24 brauchen. Identität wird einmal pro Browser-Sitzung über `demo.load` + `gr.Request` in ein `gr.State` geholt, statt `gr.Request` an jeden Handler zu hängen (die Persona-Buttons laufen über `functools.partial`). **Befund miterledigt:** `ui.web.host` steht auf `0.0.0.0`, aber das alte `share_auth` griff nur bei `share: true` — im LAN war die App ohne jede Anmeldung erreichbar; die neue Konfiguration wirkt unabhängig von `share`, `share_auth` bleibt als Fallback mit Deprecation-Warnung erhalten. Nebenbei: `resolve_secret` stand identisch in `api/openai_compat.py` und `email_adapter/service.py` — mit dem Auth-Provider wäre es die dritte Kopie geworden, jetzt einmal in `core/utils.py`. Im Browser verifiziert: Login erscheint, falsches Passwort wird abgewiesen, richtiges kommt durch, und der Name steht danach im frisch geschriebenen Gesprächslog. 27 Tests. **Ehrlich dazu:** Basic-Auth über HTTP ist Klartext — ohne TLS eine Trennung, kein Schutz. **Offen:** API und Mail-Adapter behalten ihre eigenen Mechanismen (→ #53a).** | Technical foundation |
| 28 | Gast-Persona (V1) | **DONE (2026-07-31): Karte „Gast anlegen 🎭" im Startraster öffnet ein Formular mit Name, System-Prompt und Temperatur-Slider; die Persona lebt **nur im Sitzungsspeicher** — kein YAML, kein Reload, kein Anfassen der Kette Config → personas.py → Factory-Cache. Genau deshalb hört V1 hier auf. Neuer Pfad `AppFactory.get_streamer_for_guest`: teilt sich mit `get_streamer_for_persona` einen `_build_streamer`, wodurch Guard, Wiki, Kontext-Management, Statuszeile, Quellen und Gesprächslog gratis mitkommen statt nachgebaut zu werden. Damit der Gast dieselbe Drei-Zeitstempel-Zeile (#19) bekommt, wurde aus `_system_prompt_with_date` der reine Textteil als `with_timestamps(base, cfg)` herausgezogen — beide Pfade rufen ihn auf. Der Gast hat kein Portrait, die Bildkomponente wird ausgeblendet statt einen leeren Rahmen zu zeigen. Leerer Name oder leerer Prompt → Hinweis statt Start. Im Browser verifiziert. 9 Tests. **Offen (V2):** Persistenz nach `ensembles/custom/`.** | UX/Cool |
| 54 | Gesprächs-Persistenz statt Logfile-Missbrauch | **DONE (2026-07-31): das Projekt benutzte sein Gesprächs-*Logfile* als Ablage. Konkret hieß das: zwei unvereinbare Formate (JSONL im Archiv, `{meta, messages}` beim Download — der eingebaute `load_conversation` konnte das Archiv gar nicht lesen), ein Gespräch war kein eigenständiges Ding sondern hing an der Lebensdauer eines Streamer-Objekts, und Filtern oder Suchen hätte bedeutet, alle Dateien zu lesen. Neues `src/storage/` mit `ConversationStore`-Protokoll, `SqliteStore` (Default) und `NullStore`. **SQLite über die stdlib**, weil #49 damit einen FTS5-Index geschenkt bekommt (verifiziert vorhanden), #24 ohnehin eine zweite Tabelle braucht und es keine neue Abhängigkeit ist. Migrationen über `PRAGMA user_version` plus eine Liste von SQL-Schritten — ~15 Zeilen statt Framework; die FTS-Tabelle wird bewusst erst Schritt 2 mit #49, einen Index vor dem Sucher zu bauen wäre Vorratshaltung. Der JSONL-Mitschnitt bleibt als **opt-in Debug-Artefakt** (`logging.conversation_jsonl`, Default aus). Gespräche haben jetzt eine ID, die die Oberfläche hält (`gr.State`) statt der Streamer — dadurch schreibt ein fortgesetztes Gespräch in denselben Datensatz. Aufzeichnen bricht nie den Stream, und eine unbrauchbare Datei degradiert zum NullStore statt den Start zu verhindern. Alte Logs werden **nicht** migriert (abgesprochen). Beim Nachpolieren gefunden und behoben: Terminal, API und Mail-Adapter eröffneten kein Gespräch und zeichneten deshalb **gar nichts** mehr auf, seit der JSONL-Mitschnitt aus ist — jetzt über `AppFactory.open_conversation` für alle vier Kanäle. Ebenfalls dabei: die Methode `list` beschattete in den Annotationen derselben Klasse den Builtin (`-> list[ConversationRef]` löste auf die Methode auf) — umbenannt zu `list_conversations`, von mypy gefunden. In einer zweiten Runde dazu: das Bestätigungs-Häkchen fürs Löschen überlebte den Weg zur Startseite (der nächste Klick hätte ohne neue Bestätigung gelöscht — Slot fehlte im Reset), und der Datei-Austausch ist über `storage.file_exchange` abschaltbar, in beiden Oberflächen. 24 Tests. **Korrektur zu meiner eigenen Begründung:** ich hatte den Modellwechsel mitten im Gespräch als Beleg angeführt — das Modell-Dropdown steckt aber im „Erweitert"-Akkordeon der Startseite und ist während eines Chats gar nicht erreichbar. Der Fall ist über die UI nicht auslösbar; die Verdrahtung bleibt trotzdem korrekt.** | Technical foundation |
| 25 | Konversations-Management im WebUI | **DONE (2026-07-31, auf #54): Karte „Verlauf öffnen 🗂" listet die Gespräche des **angemeldeten Nutzers** — genau dafür kam #53 vorher, ohne Nutzerfilter zeigt eine Verlaufsliste jedem alles. Auswahl über ein `gr.Dropdown` (`2026-07-31 08:11 · PETER · Wie ist der Status …`), bewusst **kein `gr.Dataframe`**: die Komponente verliert in Gradio 4.44 Streaming-Updates und verfälscht mit ihren Mess-Zeilen Browser-Tests. Vorschau als Markdown, drei Aktionen: Öffnen (lädt in den Chat und macht ihn **fortsetzbar** — dieselbe Gesprächs-ID, also schreibt die Fortsetzung in denselben Datensatz statt einen neuen anzulegen), Als Markdown (Download) und Löschen. Gespräche von Gast-Personas bleiben lesbar, aber nicht fortsetzbar — der Prompt lebte nur in ihrer Sitzung; das wird gesagt statt still zu scheitern. Löschen verlangt ein Häkchen — es ist endgültig, und ein Fehlklick darf nichts vernichten. Im Browser Ende zu Ende verifiziert (ohne Häkchen bleibt das Gespräch, mit Häkchen ist es weg und das Häkchen zurückgesetzt). 13 Tests.** | UX |
